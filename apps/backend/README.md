# LoreDeck backend

## Static card images

The Game API serves files from `static/` at `/static/`. Card `image_path` values remain
relative to that directory, so `cards/major-arcana/example.webp` is publicly available at
`/static/cards/major-arcana/example.webp`.
